# Run lint-staged to check and fix staged files
npx lint-staged

# Note: Type checking temporarily disabled due to remaining Svelte 5 compatibility issues
# npm run check

# Run tests
npm test -- --run
