module.exports = {
    plugins: [
        require('postcss-nested'),
        require('postcss-sorting'),
        require('autoprefixer'),
        require('cssnano'),
        require('postcss-size'),
    ],
}
