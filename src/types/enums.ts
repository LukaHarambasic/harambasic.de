export enum SortProperty {
	Date = 'DATE',
	Title = 'TITLE',
	Count = 'COUNT',
	Display = 'Display',
}

export enum SortDirection {
	Desc = 'DESC',
	Asc = 'ASC',
}

export enum ProjectStatus {
	Active = 'ACTIVE',
	Inactive = 'INACTIVE',
	Paused = 'PAUSED',
}
