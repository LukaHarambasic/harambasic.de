---
import ListsList from '../../components/Lists/ListsList.svelte';
import DefaultLayout from '../../layouts/DefaultLayout.astro';
import { rawToLists } from '../../util/data/lists';
import { getPath } from '../../util/helper';

const title = 'Lists';
const description = 'TBD';
const permalink = 'TBD';
const socialImg = 'TBD';
const socialImgAlt = 'TBD';

const rawLists = await Astro.glob('../../content/lists/*.md');
const lists = rawToLists(rawLists);
---

<DefaultLayout
	title={title}
	description={description}
	permalink={permalink}
	socialImg={socialImg}
	socialImgAlt={socialImgAlt}
>
	<ListsList client:load lists={lists} />
</DefaultLayout>
