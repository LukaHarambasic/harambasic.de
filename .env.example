# Secret Content System Configuration

# CRITICAL: Master password for encrypting/decrypting secret content
# This should be a strong, randomly generated password
# NEVER commit the actual .env file with real values to version control
MASTER_PASSWORD=your-secure-master-password-change-this

# Database Configuration (for production session storage)
# DATABASE_URL=postgresql://username:password@localhost:5432/secretcontent

# Redis Configuration (for production rate limiting and sessions)  
# REDIS_URL=redis://localhost:6379

# Security Configuration
# SESSION_SECRET=your-session-secret-key-change-this
# BCRYPT_ROUNDS=12

# SMTP Configuration (for security alerts)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=alerts@yourdomain.com
# SMTP_PASS=your-smtp-password
# ALERT_EMAIL=security@yourdomain.com